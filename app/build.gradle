/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java application project to get you started.
 * For more details on building Java & JVM projects, please refer to https://docs.gradle.org/9.0.0/userguide/building_java_projects.html in the Gradle documentation.
 */

 /**
 * Proyecto: Expresso - Transpilador de lenguaje Expresso a Java
 * Curso: [EIF400-II-2025] Paradigmas de Programacion
 * Universidad Nacional de Costa Rica
 * 
 * Autores:
 * - Kendall Miso Chinchilla Araya  -   119310542
 * - Ignacio Serrano Gonzalez       -   402600631
 * - Minor Brenes Aguilar           -   116730106
 * - Pablo Chavarria Alvarez        -   117810573
 * 
 * Codigo de grupo: 02-1PM
 * 
 * Nota: Este codigo fue generado inicialmente por Gradle, 
 * con adiciones de asistencia de IA, y posteriormente 
 * modificado, adaptado y validado por el equipo
 * de desarrollo para cumplir con los requerimientos especificos
 * del proyecto.
 */

plugins {
    // Apply the application plugin to add support for building a CLI application in Java.
    id 'application'
    id 'java'
    id 'antlr'
    //test
}

repositories {
    // Use Maven Central for resolving dependencies.
    mavenCentral()
}

dependencies {
    // Use JUnit test framework.
    testImplementation 'junit:junit:4.13.2'

    // This dependency is used by the application.
    implementation libs.guava

    implementation 'info.picocli:picocli:4.7.6'
    annotationProcessor 'info.picocli:picocli-codegen:4.7.6'

    antlr "org.antlr:antlr4:4.13.2" // For the ANTLR tool
    //implementation "org.antlr:antlr4-runtime:4.13.2" // For the ANTLR runtime in your application
}

generateGrammarSource {
    maxHeapSize = "64m"
    arguments += ['-package', 'main.antlr4'] // Opcional: especifica el paquete
    outputDirectory = file("${project.buildDir}/generated-src/antlr/main/java/una/paradigmas/antlr")
}
 
sourceSets {
    main {
        java {
            srcDirs += ["${project.buildDir}/generated-src/antlr/main"]
        }
    }
}

// Apply a specific Java toolchain to ease working on different environments.
java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(23)
    }
}

application {
    // Define the main class for the application.
    mainClass = 'una.paradigmas.App'
}

task copyDependencies(type: Copy) {
    from configurations.runtimeClasspath
    into 'build/libs' 
}

task jpackageImage(type: Exec) {
    dependsOn jar
    dependsOn copyDependencies
    
    commandLine 'jpackage',
        '--name', 'expressor',
        '--input', 'build/libs',
        '--main-jar', "${jar.archiveFileName.get()}",
        '--main-class', 'una.paradigmas.App',
        '--type', 'app-image',
        '--dest', 'build/jpackage',
        '--verbose',
        '--win-console',
        '--java-options', '-Dfile.encoding=UTF-8',
        '--java-options', '-DPROJECT_ROOT=../../../../'
}